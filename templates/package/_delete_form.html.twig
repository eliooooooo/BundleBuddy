<form method="post" action="{{ path('app_package_delete', {'id': package.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ package.id) }}">
    <button class="btn">Delete</button>
</form>
